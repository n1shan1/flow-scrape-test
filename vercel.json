{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "bun run build",
  "installCommand": "bun install && PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true bunx puppeteer browsers install chrome"
}
